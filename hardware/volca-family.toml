schema_version = 1
id = "volca-family"
name = "Korg Volca Bass, Keys, and Drum"
roles = ["external-synth", "external-drums", "midi-clock-receiver"]
verification = "hardware"
notes = [
  "Rig channels are explicit project choices, not assumed factory defaults.",
  "Confirm each unit's receive channel and clock setting before the first jam.",
  "Record audio-input gain and latency observations during acceptance testing.",
]
